@model List<StudentLibrary.Models.Student>

@{
    ViewData["Title"] = "Student List";
}

<h2 class="text-center my-4">Registered Students</h2>

<div class="container">
    <table class="table table-bordered table-striped table-hover align-middle text-center">
        <thead class="table-dark">
            <tr>
                <th>Roll No</th>
                <th>Full Name</th>
                <th>Father's Name</th>
                <th>DOB</th>
                <th>Mobile</th>
                <th>Email</th>
                <th>Gender</th>
                <th>Department</th>
                <th>Course</th>
                <th>City</th>
                <th>Photo</th>
                <th>Address</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var student in Model)
            {
                <tr>
                    <td>@student.RollNo</td>
                    <td>@student.FullName</td>
                    <td>@student.FatherName</td>
                    <td>@student.DOB.ToString("dd-MM-yyyy")</td>
                    <td>@student.MobileNo</td>
                    <td>@student.EmailId</td>
                    <td>@student.Gender</td>
                    <td>@student.Department</td>
                    <td>@student.Course</td>
                    <td>@student.City</td>
                    <td>
                        @if (!string.IsNullOrEmpty(student.PhotoPath))
                        {
                            <img src="@student.PhotoPath" width="80" height="80" class="img-thumbnail" />
                        }
                    </td>
                    <td>@student.Address</td>
                    <td>
                        <a href="/StudentMVC/Edit?id=@student.StudentId" class="btn btn-sm btn-warning mb-1">Edit</a>
                        <a href="/StudentMVC/Delete?id=@student.StudentId" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <a href="/StudentMVC/Index" class="btn btn-success mt-3">Create New</a>
</div>
